# GyanGrit — Base Requirements
# Installed in all environments (dev + prod)

Django>=4.2,<5.0
djangorestframework>=3.14
django-cors-headers>=4.3        # CORS middleware — required for frontend/backend separation
psycopg[binary]>=3.1            # PostgreSQL adapter (binary for easier install)
python-dotenv>=1.0              # .env file loading in base.py
openpyxl>=3.1                   # Excel parsing for roster uploads (roster/services.py)
