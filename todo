GyanGrit TODO — updated 2026-03-22

See tasks.md for the full session-by-session history.

========================================
DONE THIS SESSION (2026-03-22)
========================================

[x] BUG: teacher_course_analytics returning ALL 60 courses
    Root cause: scope_queryset() follows institution path for TEACHER,
    not subject assignment path. Fixed to filter by subject_id__in.
    File: backend/apps/content/views.py → teacher_course_analytics()

[x] BUG: AdminAssessmentBuilderPage back button → hardcoded /admin/content/
    Root cause: navigate() called with hardcoded /admin path
    Fix: useLocation() prefix detection (/teacher, /principal, /admin)
    File: frontend/src/pages/AdminAssessmentBuilderPage.tsx

[x] BUG: NavMenu dead-end items (TEACHER/PRINCIPAL "Notes" group)
    Fix: cleaned up labels, added "via dashboard" badges, footer explanation

[x] BUG: PrincipalDashboardPage class cards → /teacher/classes/:id (wrong namespace)
    Fix: now uses /principal/classes/:id
    File: frontend/src/pages/PrincipalDashboardPage.tsx

[x] BUG: TeacherClassDetailPage back nav + gradebook link hardcoded to /teacher
    Fix: useLocation prefix detection
    File: frontend/src/pages/TeacherClassDetailPage.tsx

[x] BUG: TeacherStudentDetailPage back nav hardcoded to /teacher
    Fix: useLocation prefix detection
    File: frontend/src/pages/TeacherStudentDetailPage.tsx

[x] ROUTER: /profile was STUDENT-only → moved to shared (all roles)
    ROUTER: added /principal/classes/:classId + /students/:studentId routes
    File: frontend/src/app/router.tsx

[x] SETTINGS: prod.py rewritten from scratch
    - reads ALLOWED_HOSTS, CORS_ALLOWED_ORIGINS from env
    - SESSION_COOKIE_SAMESITE = "None" for cross-origin Vercel→Render
    - SECURE_SSL_REDIRECT, HSTS, SECURE_PROXY_SSL_HEADER
    - structured logging to Render log stream
    File: backend/gyangrit/settings/prod.py

[x] SETTINGS: base.py — WhiteNoise added to MIDDLEWARE (after SecurityMiddleware)
    File: backend/gyangrit/settings/base.py

[x] requirements/prod.txt — added dj-database-url + whitenoise[brotli]
[x] backend/.env.example — full env var template created
[x] backend/build.sh — Render build script (collectstatic + migrate)
[x] frontend/vercel.json — SPA rewrites (fixes page refresh 404)
[x] .gitignore — !.env.example added so template is committed
[x] tasks.md — session-by-session task tracker created
[x] docs/BACKEND_DEPLOYMENT_RENDER.md — full Render deploy guide
[x] docs/FRONTEND_DEPLOYMENT_VERCEL.md — full Vercel deploy guide


========================================
VERIFY NEXT TIME YOU OPEN THE APP
========================================

[ ] Teacher dashboard → Course Completion section
    Should show ONLY teacher's assigned subjects (not all 60)
    Was: Biology Class 6, Chemistry Class 6... (all grades, all subjects)
    Should be: e.g. "Maths — Class 8", "Maths — Class 9" only

[ ] Open /teacher/courses/:courseId/assessments
    Click "Back to Lessons" → should go to /teacher/courses/:courseId/lessons
    Was: going to /admin/content/courses/:courseId/lessons (404 for teacher)

[ ] Open /principal → click a class card
    Should navigate to /principal/classes/:classId (not /teacher/classes/:id)
    Then Gradebook button should go to /principal/classes/:classId/gradebook

[ ] /profile should work when logged in as TEACHER, PRINCIPAL, OFFICIAL, ADMIN
    Was: only accessible as STUDENT

[ ] E2E: Gradebook
    Teacher → /teacher/classes/:id/gradebook
    Add mark (18/25) → should show 72% in expandable row
    Edit → should update in place
    Delete → should remove from row

[ ] E2E: Notification send
    Teacher sends to a class → Student receives in inbox


========================================
UPCOMING NEW WORK
========================================

[ ] Competition Rooms (Ably integration)
    See tasks.md for architecture decisions
    Steps:
    1. Sign up at ably.com → get API key → add ABLY_API_KEY to .env
    2. pip install ably (in requirements/base.txt)
    3. New Django app: backend/apps/competitions/
    4. Backend endpoint: POST /api/v1/realtime/token/ → Ably JWT vend
    5. npm install ably in frontend
    6. New page: CompetitionRoomPage.tsx

[ ] Chat Rooms (Ably integration — same setup as above)
    New Django app: backend/apps/chatrooms/
    New page: ChatRoomPage.tsx

[ ] Deploy backend to Render
    Follow docs/BACKEND_DEPLOYMENT_RENDER.md step by step

[ ] Deploy frontend to Vercel
    Follow docs/FRONTEND_DEPLOYMENT_VERCEL.md step by step

[ ] NavMenu redesign (post-capstone)
    Replace with sidebar drawer for staff roles
    Remove from student view

[ ] AI chatbot (post-capstone)
[ ] PWA / offline sync (post-capstone)
