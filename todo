TODO

http://127.0.0.1:5173/assessments/5 for this
can we make it assessments/10(class)/punjabi(subject)/assessmentid maybe?
please take care of that

http://127.0.0.1:5173/courses?subject_id=1 this is okay
http://127.0.0.1:5173/courses/50 can we make this something like
courses/punjabi10 or courses/10punjabi or courses/10/punjabi
as it will be easy for admin and all


hey how the lessons are currently being alloted to students?
we should allocate the instance of it i mean so that teachers, principals
can edit the lessons and assessments accordingly
rather than one change and whole main lessones only changes for every school this should not happen

Backend:
  1. apps/media/r2.py              ← extend with presign_upload() function
  2. apps/media/views.py           ← add presign endpoint
  3. apps/media/api/v1/urls.py     ← add route
  4. apps/notifications/models.py  ← add link URLValidator, attachment_name sanitizer

Frontend:
  5. services/media.ts             ← add presignUpload(), upload via XHR with progress
  6. components/NotificationDetailModal.tsx  ← safe Markdown, attachment, link handling
  7. components/NotificationPanel.tsx        ← click opens modal instead of navigating
  8. pages/NotificationsPage.tsx             ← accordion expand + file uploader widget