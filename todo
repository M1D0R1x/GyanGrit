GyanGrit TODO — updated 2026-03-23

See tasks.md for the full session-by-session history.

========================================
CURRENT STATUS
========================================

Backend:   Render (Singapore) — always-on via keep-alive ping
Frontend:  Vercel (Mumbai edge)
Database:  Supabase (Mumbai)
OTP:       Fast2SMS fully implemented — needs FAST2SMS_API_KEY set in Render env vars
Keep-alive: Frontend pings /health/ every 10 min — Render never sleeps


========================================
DONE THIS SESSION (2026-03-23)
========================================

[x] Removed Fly.io files (Dockerfile, fly.toml, BACKEND_DEPLOYMENT_FLYIO.md)
    Fly.io blocked new Indian accounts with ₹900 verification fee — skipped
    Staying on Render Singapore (same latency as any other option)

[x] Keep-alive ping added to AuthContext.tsx
    Pings /api/v1/health/ every 10 min in production only
    Prevents Render cold starts without needing a paid plan
    File: frontend/src/auth/AuthContext.tsx

[x] Fast2SMS OTP — already implemented in services.py, wired in views.py
    Just needs FAST2SMS_API_KEY added to Render environment variables

[x] Vercel TS error fixed (AdminLessonEditorPage uploadFile arg order)
[x] health endpoint: @login_required removed
[x] prod.py: defensive ALLOWED_HOSTS + CORS parsing


========================================
IMMEDIATE — DO THESE NOW
========================================

[ ] Add FAST2SMS_API_KEY to Render environment variables
    Go to: Render → gyangrit-backend → Environment → Add variable
    Key: FAST2SMS_API_KEY
    Value: your Fast2SMS API key
    Then redeploy

[ ] Verify Render is live:
    https://gyangrit.onrender.com/api/v1/health/ → should return {"status":"ok"}


========================================
VERIFY IN BROWSER (smoke tests)
========================================

[ ] Teacher dashboard → Course Completion shows ONLY assigned subjects
    (was returning all 60 courses — fixed in views.py)

[ ] Assessment builder back button works for teacher
    /teacher/courses/:id/assessments → back → /teacher/courses/:id/lessons

[ ] Principal class card navigates to /principal/classes/:id

[ ] /profile works for TEACHER, PRINCIPAL, OFFICIAL, ADMIN

[ ] E2E: Gradebook
    Teacher → /teacher/classes/:id/gradebook
    Add mark 18/25 → should show 72%
    Edit → updates in place
    Delete → removes from row

[ ] E2E: Notification send
    Teacher sends broadcast to a class
    Student in that class → notification appears in inbox

[ ] E2E: OTP SMS (after FAST2SMS_API_KEY is set)
    Teacher login → OTP arrives as SMS on mobile_primary


========================================
UPCOMING FEATURES
========================================

[ ] Competition Rooms (Ably Pub/Sub)
    1. ably.com → create app → copy API key → add ABLY_API_KEY to Render env
    2. pip install ably → add to requirements/base.txt
    3. New Django app: backend/apps/competitions/
    4. Endpoint: POST /api/v1/realtime/token/ → Ably JWT for the user
    5. npm install ably
    6. New page: frontend/src/pages/CompetitionRoomPage.tsx

[ ] Chat Rooms (Ably Chat SDK)
    Same Ably app/key as competition rooms
    npm install @ably/chat
    New Django app: backend/apps/chatrooms/
    New page: frontend/src/pages/ChatRoomPage.tsx

[ ] NavMenu redesign — post-capstone sidebar/drawer for staff roles
[ ] PWA / offline sync — post-capstone
[ ] AI chatbot — post-capstone
