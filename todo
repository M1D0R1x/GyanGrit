# GyanGrit — Bug Reports

> Bug reports only. Planned work lives in tasks_opus4.6.md.

---

## Open Bugs

- [ ] **Gemini AI chatbot 403** — API key `AIzaSyDAAUFrK6SsRf7AcXR72SIBS3su03BJCno` is blocked by Google. Veera must regenerate at https://aistudio.google.com/app/apikey → update GEMINI_API_KEY in Render env vars.
- [ ] **Push not working on friend's Chrome/Edge** — user must click "Allow" on notification prompt. If dismissed: browser Settings → Site Permissions → gyangrit.site → Notifications → Allow. Not a code issue.

---

## Resolved Bugs

| Date | Bug | Fix |
|---|---|---|
| 2026-03-26 | Production 500 on login — gevent + CONN_MAX_AGE=600 thread crash | CONN_MAX_AGE=0 + post_fork DB reset in gunicorn.conf.py |
| 2026-03-26 | Vercel build fail — LiveSessionPage onAction type error | Wrapped handleJoin as (sess) => handleJoin(sess.id) |
| 2026-03-26 | 302 redirect on chat/notifications API calls | Replaced all @login_required → @require_auth across all apps |
| 2026-03-26 | Failed to fetch dynamically imported module | 3-layer fix: lazyRetry + ChunkErrorBoundary + vite:preloadError |
| 2026-03-26 | Ably Auth() Warning: token literal | AuthContext: authCallback with `as any` |
| 2026-03-26 | TS2769 Ably authCallback type mismatch | `as any` cast on callback + options |
| 2026-03-26 | ESLint unused event param | Removed parameter entirely `() => {}` |
| 2026-03-26 | Live session notifications not in bell panel | `_notify_students_inapp` creates Broadcast + Notification + push |
| 2026-03-26 | Section dropdown 230 items, no school filter | School dropdown filters sections for ADMIN/PRINCIPAL |
| 2026-03-26 | Students can't unmute/camera in live sessions | `canPublish: true` for all + ControlBar with mic/camera controls |
| 2026-03-26 | Students can only reply, not post in chat rooms | `_user_can_post` now returns True for students in subject rooms |
| 2026-03-26 | "Session has ended" raw 400 error | User-friendly messages + auto-refresh session list |
| 2026-03-26 | Vercel Analytics ERR_BLOCKED_BY_CLIENT | NOT A BUG — ad blocker |
| 2026-03-26 | 503 on dashboard | Render cold start — keep-alive already implemented |

---

## Remaining Feature Requests (post-capstone)

- Admin chat rooms grouped by school
- Whiteboard integration (Excalidraw)
- Notification caching (incremental fetch)
- Notification reminders (10/5/15 min before live)
- ASUS ROG as server deployment guide
- Custom "purpose" field for live sessions
- Metrics collection (engagement, watch time)
